@page
@model CountryInfoApp.Pages.CountryModel
@{
    ViewData["Title"] = "Countries";
}

<h2>@ViewData["Title"]</h2>
<ul>
    @foreach (var country in Model.Countries)
    {
        <li>@country.Name - @country.Capital - @country.Continent</li>
    }
</ul>